flarum-ext-auth-ldap:
  forum:
    log_in: => core.ref.log_in
    log_in_with: Log in with LDAP
    log_out: => core.ref.log_out
  admin:
    description: Set LDAP domain
    settings:
      title: LDAP settings
      address: LDAP's domain (without "https://" or trailing "/")
      onlyUse: Disable Flarum login and only use LDAP authentication
    buttons:
      save: Save
